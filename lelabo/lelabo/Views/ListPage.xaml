<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="lelabo.Views.ListPage"
             Title="Liste">
    <ContentPage.Content>
        <ListView x:Name="maListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand" VerticalOptions="Center" Margin="15,0">
                            <Label Text="{Binding Nom}" HorizontalOptions="FillAndExpand" TextColor="Black"/>
                            <Label FontAttributes="Bold" Text="{Binding Prix}" TextColor="Black"/>
                        </StackLayout>
                    </ViewCell> 
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>